interface MenuItemProps {
    data: {
        heading: string
        listItem: string[]
    }
}
const MenuItem: React.FC<MenuItemProps> = ({ data }) => {
    return (
        
    );
}

export default MenuItem;