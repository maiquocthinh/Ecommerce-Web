export interface needType {
    id: number;
    title: string;
    description: string;
    updatedAt: string;
    createdAt: string;
}
export interface brandType {
    id: number;
    name: string;
    description: string;
    updatedAt: string;
    createdAt: string;
}
export interface categoryType {
    id: number;
    name: string;
    description: string;
    updatedAt: string;
    createdAt: string;
}
